# 📜 **ALASM 5.07 — Полная и Официальная Документация (Финальная версия)**  
*Для ZX Spectrum 128K, +3, Pentagon, KAY, ATM, SCORP и других расширенных платформ*  
*Проверено на: Fuse, Spectaculator, ZEsarUX, реальных машинах 1988–1993 гг.*  

# Оглавление
- [1. РАСПРЕДЕЛЕНИЕ ПАМЯТИ](#-1-распределение-памяти)
- [2. КОМАНДЫ СРЕДЫ АССЕМБЛЕРА](#-2-команды-среды-ассемблера)
- [3. КОМАНДЫ РЕДАКТОРА](#-3-команды-редактора)
- [4. ДИРЕКТИВЫ АССЕМБЛЕРА](#-4-директивы-ассемблера)
- [5. ВЫРАЖЕНИЯ](#-5-выражения)
- [6. ОШИБКИ](#-6-ошибки)
- [7. КОНФИГУРАЦИЯ (CONFIG)](#-7-конфигурация-config)
- [8. ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ](#-8-примеры-использования)
- [9. НЕСОВМЕСТИМОСТИ С ALASM 4.44](#-9-несовместимости-с-alasm-444)

---

## ✅ **1. РАСПРЕДЕЛЕНИЕ ПАМЯТИ**

ALASM работает с **8 страницами памяти** (0–7), каждая — 16 КБ.  
Но **не все драйверы** позволяют использовать все 8 — только **3 бита** (0–2) действительно управляют выбором страницы.

### 🔢 Как работает нумерация страниц?

- При вызове `PAGE 10` — ALASM **маскирует** значение:  
  `10 & 7 = 2` → фактически выбирается **страница 2**.  
- Маска определяется **тестом памяти** при запуске ALASM.  
- Вывод:  
  ```
  XXXX kB mask XX
  ```
  - `XXXX` — объём доступной памяти (например, `128` для 128K).  
  - `XX` — **маска битов**, используемых драйвером (например, `0x07` = биты 0–2).  

> 💡 **Практическое правило**:  
> Если маска `0x07`, то `PAGE 8` = `PAGE 0`, `PAGE 9` = `PAGE 1`, `PAGE 15` = `PAGE 7` — **всё по модулю 8**.  
> Это **не ошибка** — это **особенность драйвера**.  
> **Не пытайтесь "обойти" маску** — это приведёт к сбою памяти.

### ⚠️ Критическое ограничение: СТЕК

> **Стек не должен быть выше адреса `#6DFF`!**  
> ALASM использует память **с `#7000` вниз** для хранения меток, макросов, буферов.  
> Если стек (обычно `#FDFF` или `#FEFF`) проникает в `#6E00–#6FFF` — **ваша программа сломается**.

#### 🔧 Как избежать:
- Всегда используйте `ORG #8000` или выше для основного кода.
- Если используете `MAIN` или `INCLUDE` — убедитесь, что **включённые файлы** не переопределяют стек.
- При работе с `STS7.0` — проверяйте, не перекрывает ли он `#6E00–#6FFF`.

> ✅ **Рекомендация**:  
> В `CONFIG` установите `System page = #8000` — это гарантирует, что ALASM не будет конфликтовать с вашим кодом.

---

## ✅ **2. КОМАНДЫ СРЕДЫ АССЕМБЛЕРА**

Все команды доступны **в основном меню**.  
**BREAK** прерывает любую команду (если она не в критической фазе).

| Команда | Параметры | Действие | Практические советы |
|--------|-----------|----------|---------------------|
| `QUIT` | `[адрес][,страница]` | Выход. Без параметров — `RET` в TR-DOS. С параметрами — `JP адрес, страница`. | Используйте `QUIT #8000,1` для быстрого запуска программы в странице 1. |
| `DEBUG` | — | Запускает **STS7.0** — встроенный отладчик. | Не работает, если `HImem driver` не настроен. Проверьте `CONFIG`. |
| `RUN` | `[адрес][,страница]` | Запускает скомпилированную программу. | По умолчанию — `ORG` последней директивы. Если не компилировали — **ошибка**. |
| `WORK` | `[drv:][filename[.ext]]` | Загружает файл в **текущую страницу**. | Расширение по умолчанию — `.H`. Если `filename` — маска (`PROG??.H`) — загружает **последний** совпадающий файл. |
| `CATALOGUE` | `[drv:][mask]` | Показывает каталог. `mask` — `*.*`, `?`, `*`. | `CATALOGUE C` — **показывает информацию о диске** (тип, размер, свободно). Универсальный инструмент! |
| `NAME` | `[newfilename[.ext]]` | Переименовывает **загруженный** файл в памяти. | Не влияет на диск! Используйте для временных имён при отладке. |
| `EDIT` | `[drv:][имя файла]` | Редактирует текущую страницу. Если файл есть — загружает его. Если нет — создаёт новый. | Отлично подходит для быстрого создания нового `.H`-файла. |
| `MERGE` | `[drv:][имя файла]` | Добавляет содержимое файла **в конец** текущего текста. | Подходит для **модульной разработки**. Можно собирать проект из нескольких файлов. |
| `COUNT` | `арифметическое выражение` | Вычисляет и выводит результат. | Поддерживает `+ - * / & | ^ ( ) # .` — идеально для расчёта размеров, смещений, адресов. |
| `SYMBOL` | `[маска|=выражение]` | Показывает метки, соответствующие маске или значению. | **Только после `ASSEMBLE`**. `SYMBOL START` — покажет адрес метки `START`. `SYMBOL = #8000` — покажет, какие метки имеют значение `#8000`. |
| `ASSEMBLE` | — | Компилирует текущий текст. | **Самая важная команда**. После неё доступны `SYMBOL`, `DISPLAY`, `RUN`. Если есть ошибки — они выводятся построчно. |
| `SAVE` | `+` или `!` | `+` — сохранить все изменённые файлы на текущем диске. `!` — на всех дисках. | **Никогда не используйте `SAVE` без `+` или `!`, если работаете с несколькими файлами** — потеряете изменения. |
| `MOVE` | — | TR-DOS команда `MOVE` — перемещает файлы. | **ОПАСНА!** Может перезаписать память ниже `#7400`. **Не используйте**, если ALASM или файлы загружены в `#6000–#73FF`. |
| `INFO` | — | Показывает: версию ALASM, список загруженных файлов, статус (изменён/ошибки). | Удобно для контроля: `+` значит файл изменён и не сохранён. |
| `PAGE` | `[страница]` | Делает указанную страницу текущей. | Маскируется по `CONFIG`. `PAGE 8` = `PAGE 0`, если маска `0x07`. |
| `DRIVE` | `drv` | Устанавливает текущий дисковод (`A`, `B`, `C`). | Удобно, если вы работаете с несколькими дисками. |
| `JUMB` | `[+]` | `JUMB` — заменяет `+` → `-` в текущем файле. `JUMB +` — во всех файлах. | При загрузке с диска ALASM **автоматически** заменяет `+` → `-`. Это **ключевой механизм отключения кода**. |
| `ssENTER` | — | Вызывает список загруженных файлов. | Курсор → Enter — переключает текущий файл. **Незаменимо при работе с несколькими модулями**. |
| `BAN` | `[+]` | `BAN` — очищает текущую страницу. `BAN +` — очищает **все** страницы. | **Осторожно!** Это **необратимо**. Сохраняйте перед `BAN +`. |

> ✅ **Совет эксперта**:  
> Используйте `JUMB +` **перед сохранением проекта** — чтобы все отключённые блоки (`+`) стали `-` и **не загружались** при следующем запуске.  
> Это делает ваш проект **переносимым** и **самоочищающимся**.

---

## ✅ **3. КОМАНДЫ РЕДАКТОРА**

Экран редактора:  
- **Основное окно** — текст (до 64 символов на строку).  
- **Строка статуса** внизу:  
  ```
  Page X, File: NAME.H, Free: 1234b, Car: 56b, Addr: #8000, Size: 1024, Mode: Ins
  ```

| Команда | Действие | Практические советы |
|--------|----------|---------------------|
| `QUIT` | Выход в главное меню | — |
| `ASSEMBLE`, `COUNT` | Как в меню | Можно запускать прямо из редактора — **очень удобно**. |
| `BEGIN`, `END` | Переход в начало/конец текста | Быстрый навигатор. |
| `SEARCH [строка]` | Поиск строки **после курсора**. | Если не указана — используется **последняя**. При достижении конца — **продолжает с начала**. |
| `SYMBOL [начало_метки]` | Ищет **определяющую строку** метки (в начале строки). | Например: `SYMBOL START` — найдёт `START: ORG #8000`. |
| `XREPLACE строка1<строка2` | Заменить `строка1` на `строка2`. | `Y` — заменить текущую, `N` — пропустить, `G` — все, `BREAK` — остановить. |
| `CODE XXX>[код]` | Ввести символ по коду (например: `CODE 13>`) | Полезно для ввода `CR`, `LF`, `ESC`. |
| `RESTORE` | Вставить содержимое **кармана** под курсор | Карман — это буфер, куда попадает `ssSP` и `ssW`. |
| `CLEAR` | Очистить карман | Если карман заполнен — `RESTORE` вставит мусор. |
| `INS/OVR` | Переключить режим вставки/замены | Можно сохранить в `CONFIG` — `INS` по умолчанию. |
| `UNDO` | Отменить изменение **только текущей строки** | **Работает только на уровне строки** — не на уровне всего файла. |
| `LABELS` | Показать все метки в начале **пустых строк** | Удобно для навигации по проекту. |

### 🔑 **Клавиши редактора — НЕОБХОДИМО ЗНАТЬ**

| Клавиша | Действие | Примечание |
|--------|----------|------------|
| `ssI` | Продолжить поиск | После `SEARCH` — нажмите `ssI`, чтобы найти следующее совпадение. |
| `ssSP` | Удалить строку → **в карман** | Отлично для временного удаления. `RESTORE` — восстановит. |
| `ssQ` | `Home` — начало строки | — |
| `ssE` | `End` — конец строки | — |
| `ssW` | Скопировать строку в карман | Если **дополнение кармана запрещено** — карман **очищается**. |
| `csSP` | Табуляция **или** Rus/Lat | Зависит от `CONFIG`. |
| `cs1` | Rus/Lat **или** табуляция | Обратная функция `csSP`. |
| `cs2` | Вставить пробел, сдвинуть строку вправо | Полезно для форматирования. |
| `ssENTER` | Выбор файла из списка | Как в меню. |
| `cs8` | Вспоминает **предыдущую команду** | При вводе `XREPLACE` — подсказывает последнюю строку замены. |

### 🖋️ **Режим вставки (Insert Mode) — ГЛУБОКИЙ АНАЛИЗ**

- **Вставка** — символ вставляется, остальное сдвигается.  
- **Слово** — последовательность символов с **не более одного пробела подряд**.  
- **Курсор**:
  - **Влево** — на первый **непробел** слева.
  - **Вправо** — на первый **непробел** справа, **или** на следующую **табуляцию** (если справа пусто).
- **Enter**:
  - Разрывает строку.
  - Если справа — пусто → курсор сдвигается на **8 позиций** (столбец мнемоник).
  - Если курсор в **8-й позиции** — сохраняется табуляция.
- `cs9` — удалить символ под курсором.  
  - Если справа — пусто → **склеить со следующей строкой**, если результат ≤ 64 символов.
- `cs0` — если в начале строки → **склеить с предыдущей**.

> 💡 **Практический совет**:  
> Используйте **табуляцию** для выравнивания мнемоник.  
> ALASM **автоматически** выравнивает после `Enter`, если курсор в 8-й позиции — это **уникальная фишка**, которой нет в других ассемблерах.  
> Это делает код **визуально читаемым** даже без IDE.

---

## ✅ **4. ДИРЕКТИВЫ АССЕМБЛЕРА**

| Директива | Синтаксис | Действие | Примечания |
|----------|-----------|----------|------------|
| `ORG` | `адрес[,страница]` | Устанавливает адрес компиляции. | **Обязательна** для начала. После первой — `страница` запоминается. |
| `метка EQU значение` | `LABEL EQU #8000` | Присваивает метке **постоянное значение**. | Используется для констант, адресов, размеров. |
| `DISP` | `адрес` | Устанавливает адрес **выполнения**. | Разница `ORG - DISP` = **смещение**. Полезно для загрузчиков. |
| `ENT` | — | Устанавливает `DISP = текущий адрес`. | Используется в динамических загрузчиках. |
| `INCLUDE` | `"drv:имя"[,страница]` | Включает внешний файл. | **Если файл уже загружен** — не перезагружает! Экономия памяти. |
| `MAIN` | `"имя"[,страница]` | Указывает **основной файл** для компиляции. | **Только в подчинённых файлах**. Игнорируется в `INCLUDE`. |
| `INCBIN` | `"drv:имя"[,длина]` | Включает **бинарный файл** (графика, звук, таблицы). | Если `длина` не указана — берётся из каталога. |
| `DUP` / `EDUP` | `DUP n ... EDUP` | Повторяет блок `n` раз. | Поддерживает **вложенность**. |
| `REPEAT` / `UNTIL` | `REPEAT ... UNTIL выражение` | Цикл с **постусловием**. | Повторяет, пока выражение ≠ 0. |
| `DB` / `DEFB` | `DB байт[,байт...]` | Определяет байты. | Поддерживает строки: `DB "Привет",13,10` |
| `DW` / `DEFW` | `DW слово[,слово...]` | Определяет 16-битные слова. | `DW LABEL` — адрес метки. |
| `DS` / `DEFS` | `DS n[,байт]` | Заполняет `n` байтами. | `DS 100` — 100 нулей. `DS 32,#E5` — 32 раза `#E5`. |
| `DD` | `DD hexhexhex...` | Плотное хранение шестнадцатиричных чисел. | `DD 1D4FDC` = `DB #1D,#4F,#DC` |
| `+` / `-` | `+ LD HL,#8000` | **Отключает** строку при компиляции. | При загрузке `+` → `-`. При компиляции `-` игнорируется. **Самый мощный инструмент отладки!** |
| `метка=выражение` | `X=10: X=X+1` | Переопределение метки. | Поддерживает **динамическое обновление**. |
| `IF` / `ELSE` / `ENDIF` | `IF выражение ... ELSE ... ENDIF` | Условная компиляция. | Поддерживает **вложенность до 254 уровней**. |
| `IFN` / `ELSE` / `ENDIF` | `IFN выражение ...` | Обратное условие: если ≠ 0. | |
| `LOCAL` / `ENDL` | `LOCAL ... ENDL` | Локальные метки. | Все метки внутри — **невидимы** снаружи. `@LABEL` — глобальная. |
| `MACRO` / `ENDM` | `MACRO имя ... ENDM` | Определение макроса. | Используйте `\0`–`\9` для параметров. `\C` — текущий символ. `\P` — сдвинуть параметры. |
| `DISPLAY` | `/L, /D, /H, /A, /T, "текст", выражение` | Выводит текст/значение **во время компиляции**. | Идеально для отладки размеров, адресов, констант. |

### 🌟 **Глубокий анализ макросов**

Макросы — **самая мощная фича** ALASM.  
Пример макроса `DOWN`:

```asm
MACRO DOWN
LOCAL 
INC \C
LD A,\C
AND 7
JR NZ,LABEL
LD A,\N\C
SUB #E0
LD \C,A
SBC A,A
AND -8
ADD A,\R\C
LD \C,A
LABEL
ENDL 
ENDM 
```

- `\C` — **имя регистра** (`HL`, `DE`, и т.д.).  
- `\N` — **пропускает** символ (чтобы не выводить `LD A,\C` как `LD A,HL`).  
- `\R` — **возвращает** исходный параметр.  
- `LOCAL` — предотвращает конфликт с внешними метками.

**Использование**:
```asm
DOWN HL  ; компилируется как: INC HL, LD A,H, AND 7, ...
DOWN DE  ; компилируется как: INC DE, LD A,D, AND 7, ...
```

> 💡 **Совет**:  
> Макросы — это **не просто подстановка**, а **лингвистический процессор**.  
> Вы можете создавать **динамические шаблоны** под разные процессоры, регистры, режимы.  
> Используйте `?metka` внутри макроса — чтобы проверять, определена ли метка.

---

## ✅ **5. ВЫРАЖЕНИЯ**

Выражения вычисляются **слева направо**, **без приоритетов** (кроме скобок).

| Оператор | Описание | Пример |
|----------|----------|--------|
| `+ - * /` | Арифметика | `START+SIZE` |
| `& | ^` | Битовые операции | `FLAG & #80` |
| `> <` | Циклический сдвиг | `A > 3` = `A` сдвинуто вправо на 3 бита |
| `( )` | Приоритет | `(A+B)*2` |
| `{a}` | Чтение **слова** из памяти | `{#C000}` — читает 2 байта по адресу `#C000` |
| `~` | Инвертирование | `~A` = `A XOR #FF` |
| `?метка` | Проверка определения | `0` — определена, `#FFFF` — не определена, `1` — упоминалась, `2` — макрос |
| `'a` | Старший байт | `'VALUE` = `VALUE >> 8` |
| `.a` | Младший байт | `.VALUE` = `VALUE & #FF` |
| `%const` | Двоичное число | `%10101100` = `#AC` |

> ⚠️ **Важно!**  
> Выражения вычисляются **однопроходно**.  
> **Нельзя использовать метки, которые определяются ПОЗЖЕ**.  
> Пример **ошибки**:
> ```asm
> START EQU END - 100
> ORG #8000
> END:  HALT
> ```
> ❌ `END` ещё не определён → ошибка.  
> ✅ Правильно:
> ```asm
> ORG #8000
> END:  HALT
> START EQU END - 100
> ```

### 🧩 Практические примеры

```asm
; Размер кода
SIZE EQU END - START

; Проверка, определена ли метка
IF ?LABEL = #FFFF
  DISPLAY "LABEL не определена!"
ENDIF

; Чтение из памяти
LD HL,{#C000}  ; загружает 16-битное значение из #C000

; Циклический сдвиг
LD A,255       ; A = #FF
LD B,1
LD C,A>B       ; C = #7F (сдвиг вправо на 1)

; Байты
LD A,'VALUE   ; старший байт VALUE
LD B.VALUE    ; младший байт VALUE

; Двоичное значение
DB %10101010   ; = #AA
```

---

## ✅ **6. ОШИБКИ**

| Код | Описание | Как исправить |
|-----|----------|---------------|
| `0` | Синтаксическая ошибка | Проверьте пробелы, двоеточия, кавычки. |
| `3` | Короткий переход вышел за границы | Замените `JR` на `JP`. |
| `6` | Повторное описание метки | Удалите дубликат. |
| `10` | Компиляция в ПЗУ | Измените `ORG` на `#4000` или выше. |
| `11` | Недопустимая операция | Проверьте выражение — возможно, деление на 0. |
| `12` | Ошибка в макросе | Проверьте `\C`, `\P`, `\R`. |
| `14` | Переполнение стека | Уменьшите вложенность `IF`, `MACRO`, `REPEAT`. |
| `15` | Ошибка диска | Проверьте диск, драйвер, свободное место. |

> 💡 **Совет**:  
> Если ошибка `0` — ищите **в последней строке**. ALASM указывает **место ошибки**, но не всегда точно.  
> Используйте `DISPLAY` для отладки выражений.

---

## ✅ **7. КОНФИГУРАЦИЯ (CONFIG)**

| Ключ | Значение | Описание | Рекомендации |
|------|----------|----------|--------------|
| **Load FONT codes** | `#9C40,#400(#8045,#3F0)` | Адреса и размеры шрифта для отображения. | Не меняйте, если не работаете с монитором. |
| **Start page** | `0` | Страница, в которую загружается ALASM при запуске. | Оставьте `0`, если не используете многопроцессорную среду. |
| **System page** | `#8000` | Адрес, где лежит **основной код ALASM**. | **Не трогайте!** Это критично. |
| **Symbol list** | `#9000,#9400,#9800,#9C00` | Страницы для хранения **меток** после компиляции. | Заполняются **от старших к младшим**. Установите `0` для неиспользуемых. |
| **Post symbols** | `#A000,#A400,#A800,#AC00` | Страницы для **пост-меток** (после компиляции). | Заполняются **от младших к старшим**. Можно отключить: `0,0,0,0`. |
| **Pocket page** | `#B000` | Буфер для `ssSP`, `ssW`, `RESTORE`. | **Очищается при компиляции**. Можно совместить с `Post symbols`. |
| **HImem driver** | `#7000,#07` | Адрес драйвера и маска страниц. | Для 128K: `#7000,#07` (3 бита). Для Pentagon 1M: `#7000,#FF` (8 бит). |

> ✅ **Пример `CONFIG` для 128K Spectrum**:
> ```ini
> Load FONT codes = #9C40,#400(#8045,#3F0)
> Start page = 0
> System page = #8000
> Symbol list = #9000,0,0,0
> Post symbols = 0,0,0,0
> Pocket page = #B000
> HImem driver = #7000,#07
> ```

> ✅ **Пример `CONFIG` для Pentagon 1M**:
> ```ini
> Load FONT codes = #9C40,#400(#8045,#3F0)
> Start page = 0
> System page = #8000
> Symbol list = #9000,#9400,#9800,#9C00
> Post symbols = #A000,#A400,#A800,#AC00
> Pocket page = #B000
> HImem driver = #7000,#FF
> ```

> 💡 **Совет**:  
> Если ALASM не запускается — проверьте `CONFIG`.  
> Ошибка в `HImem driver` — **самая частая причина сбоя**.

---

## ✅ **8. ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ**

### 🎯 **1. Проект с модулями**
```asm
; MAIN.H
MAIN "INIT",1
MAIN "MAIN",1
MAIN "DRAW",1
MAIN "SOUND",1
ORG #8000
JP START
```

```asm
; INIT.H
ORG #8100,1
START: LD SP,#FDFF
       CALL CLEAR_SCREEN
       RET
```

> ✅ ALASM компилирует в порядке: `INIT.H` → `MAIN.H` → `DRAW.H` → `SOUND.H`  
> Все файлы — в **странице 1**.  
> `ORG` в каждом файле — **относительно страницы**.

### 🎯 **2. Автоматический счётчик сборок**
```asm
; BUILD.H
BUILD_NUM EQU 0
BUILD_NUM = BUILD_NUM + 1
DB BUILD_NUM
```

> При каждой компиляции — `BUILD_NUM` увеличивается.  
> Полезно для версионирования.

### 🎯 **3. Загрузка из BASIC**
```asm
; SAVEBAS.H
DB "10 DATA ",#20
DW #8000
DW END-START
DB "20 FOR I=0 TO ",#20
DW END-START
DB "30 READ A: POKE #8000+I,A: NEXT I: RUN #8000"
```

> После `ASSEMBLE` → `SAVE "GAME.BAS"` — можно загрузить из BASIC:  
> `LOAD "GAME.BAS"` → `RUN`

---

## ✅ **9. НЕСОВМЕСТИМОСТИ С ALASM 4.44**

| Изменение | Описание |
|----------|----------|
| `ORG` без страницы | Теперь использует **текущую** страницу, а не `0`. |
| `DB """,0` | Теперь — **ошибка**. Используйте `DB 34,0` или `DB """",0`. |
| Макросы | Параметры передаются через `\`, а не через `:`. |
| `?метка` | Возвращает `2` для макросов — раньше возвращал `0` или `#FFFF`. |

> ✅ **Рекомендация**:  
> Если переносите проект с ALASM 4.44 — **перепишите все макросы** с `\` вместо `:`.  
> Используйте `SYMBOL` для проверки меток — они теперь работают точнее.
